# Local defaults. Never put real tokens/passwords in this example.
PORTAL_ORIGIN=http://127.0.0.1:3000
# Preview remains local only; production login requires shared storage.
PORTAL_ENABLE_PREVIEW=true
PORTAL_ENABLE_VTEX_LOGIN=true

# Vercel Production: use the exact public origin below, disable preview,
# and connect an Upstash Redis database. Never commit real credentials.
# PORTAL_ORIGIN=https://customer-portal-volvo.vercel.app
# PORTAL_ENABLE_PREVIEW=false
# UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
# UPSTASH_REDIS_REST_TOKEN=set-in-vercel-only

# AI Assistant — WWC channel. Values read from the native widget bootstrap at
# .../apptypes/wwc/bb4a6378-c6f7-4fee-9335-0895810b9f12/script.js (that path
# carries the INTEGRATION uuid; the channel uuid below is a different value).
# The uuid is public — it ships to the browser. The channel enforces an origin
# allowlist, so each portal origin must be registered on the Weni platform.
NEXT_PUBLIC_WENI_CHANNEL_UUID=6d7f6ee7-884a-4070-b9ad-8f9212991965
NEXT_PUBLIC_WENI_FLOWS_ORIGIN=https://flows.weni.ai
NEXT_PUBLIC_WENI_SOCKET_URL=wss://websocket.weni.ai/ws
